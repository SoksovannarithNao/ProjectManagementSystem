# Copy to .env and adjust if needed:
#   cp .env.example .env
# Used by docker-compose.yml to configure the full stack (postgres, backend, frontend).

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=taskmanager
POSTGRES_PORT=5432

BACKEND_PORT=8080
FRONTEND_PORT=5173
